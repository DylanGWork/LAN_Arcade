# Documentation Assets

Screenshots in this directory are copied from QA reports when the companion app
changes. They are committed here so the GitHub README and offline wiki can show
the app without depending on ignored `qa/reports/` output.
