# Lemmings browser adapter placeholder

The real playable files are generated on the GannanNet VM by `npm run lemmings:build`. Original Lemmings data files are private VM/NFS assets and must not be committed to Git.
